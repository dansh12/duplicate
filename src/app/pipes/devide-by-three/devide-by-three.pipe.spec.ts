import { DevideByThreePipe } from './devide-by-three.pipe';

describe('DevideByThreePipe', () => {
  it('create an instance', () => {
    const pipe = new DevideByThreePipe();
    expect(pipe).toBeTruthy();
  });
});
