import { NextThreePipe } from './next-three.pipe';

describe('NextThreePipe', () => {
  it('create an instance', () => {
    const pipe = new NextThreePipe();
    expect(pipe).toBeTruthy();
  });
});
